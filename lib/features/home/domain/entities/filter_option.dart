
class FilterOption {
  String label;
  bool value;

  FilterOption(this.label, this.value);
}
class FilterRatingOption {
  String label;
  bool value;
  bool isImage;
  String image;

  FilterRatingOption(this.label, this.value,this.isImage, this.image);
}